#!/usr/bin/env php
<?php

require __DIR__.'/../vendor/autoload.php';

use Symfony\Component\Console\Application;
use Neopayment\NboInstaller\Console\NewModuleCommand;

$app = new Application('NBO Installer', '1.0.0');

$app->add(new NewModuleCommand());

$app->run();